# 关于cin.get()和cin.getline()的笔记
- cin.get(char* ar, int n)
    - 当输入的字符数量超过n时
        - 存下输入的前n-1个字符
        - cin.fail()仍为0，之后还能输入
        - 剩余字符留在缓冲区
    - 不丢弃最后的换行符
- cin.getline(char* ar, int n)
    - 当输入的字符数量超过n时
        - 存下输入的前n-1个字符
        - cin.fail()变为1，之后不再能输入
    - 丢弃最后的换行符
